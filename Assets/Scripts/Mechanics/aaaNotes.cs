using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class aaaNotes : MonoBehaviour
{
    /*
        Player
            *movement
                -A,D, space bar
            *health
                -regen in time or item?
            *death
                -animation
                -lose score
                -retry/main menu
            *attack
                -jump or mouse click?
            *states: idle, run, jump, death

        Enemy
            *movement
                -no flying
                -state affects speed and target direction
            *health
                -regen in time?
            *death
                -add to score
            *spawn?
                -multiple spawn locations?
                -random?
                -off screen only?
            *attack
                -on collision
                -only if player is grounded
            *states: passive, aggro, attack, death

        Collectable
            *add to score
            *remove from scene
            *carry over on replay?

        Score
            *game score
                -stars collected
                -enemies killed/spared?
            *temp (level) score
                -discard if level failed
                -add to game score if level succeeded

        Keybinds
            *heck


    */
    #region Variables

    #endregion

    #region Start
    private void Start()
    {

    }
    #endregion

    #region Update
    private void Update()
    {

    }
    #endregion

    #region Functions

    #endregion
}
